---
services:
  immich-postgres:
    profiles: [apps, all]
    image: pgvector/pgvector:0.8.2-pg17
    environment:
      - POSTGRES_USER=${IMMICH_DB_USERNAME:-immich}
      - POSTGRES_PASSWORD=${IMMICH_DB_PASSWORD:?Set IMMICH_DB_PASSWORD in .env, direnv,
        or Dockhand}
      - POSTGRES_DB=${IMMICH_DB_DATABASE_NAME:-immich}
      - TZ=${TZ:-Europe/Copenhagen}
    volumes: [immich_postgres_data:/var/lib/postgresql/data]
    healthcheck:
      test: [CMD-SHELL, pg_isready -U "$$POSTGRES_USER" -d "$$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks: [immich]
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M
  immich-power-tools:
    profiles: [apps, all]
    image: ghcr.io/immich-power-tools/immich-power-tools:latest
    depends_on:
      immich-postgres:
        condition: service_healthy
      immich-server:
        condition: service_healthy
    networks: [traefik_public, immich]
    environment:
      - TZ=${TZ:-Europe/Copenhagen}
      - IMMICH_API_KEY=${IMMICH_API_KEY:-}
      - IMMICH_URL=${IMMICH_URL:-http://immich-server:2283}
      - EXTERNAL_IMMICH_URL=${EXTERNAL_IMMICH_URL:-}
      - DB_USERNAME=${IMMICH_DB_USERNAME:-immich}
      - DB_PASSWORD=${IMMICH_DB_PASSWORD:?Set IMMICH_DB_PASSWORD in .env, direnv,
        or Dockhand}
      - DB_HOST=immich-postgres
      - DB_PORT=5432
      - DB_DATABASE_NAME=${IMMICH_DB_DATABASE_NAME:-immich}
      - AI_API_KEY=${AI_API_KEY:-}
      - AI_BASE_URL=${AI_BASE_URL:-https://api.openai.com/v1}
      - AI_MODEL=${AI_MODEL:-gpt-4o-mini}
      - POWER_TOOLS_ENDPOINT_URL=${POWER_TOOLS_ENDPOINT_URL:-https://immich-tools.${DOMAIN:-localhost.me}}
    volumes: [immich_power_tools_data:/app/data]
    labels:
      - sablier.enable=true
      - sablier.group=immich-power-tools
      - homepage.group=Media & Files
      - homepage.name=Immich Power Tools
      - homepage.icon=immich.png
      - homepage.href=https://immich-tools.${DOMAIN:-localhost.me}
      - homepage.description=Extra utilities for your Immich library
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

  # Web UI/API
  immich-server:
    profiles: [apps, all]
    depends_on:
      immich-postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    image: ghcr.io/immich-app/immich-server:v2.7.3
    command: [start.sh, immich]
    volumes: [immich_library:/usr/src/app/upload]
    environment:
      - DB_HOSTNAME=immich-postgres
      - DB_USERNAME=${IMMICH_DB_USERNAME:-immich}
      - DB_PASSWORD=${IMMICH_DB_PASSWORD:?Set IMMICH_DB_PASSWORD in .env, direnv,
        or Dockhand}
      - DB_DATABASE_NAME=${IMMICH_DB_DATABASE_NAME:-immich}
      - REDIS_HOSTNAME=redis
      - IMMICH_WORKERS_INCLUDE=api
    networks: [traefik_public, immich]
    labels:
      - traefik.enable=true
      - traefik.docker.network=traefik_public
      - traefik.http.routers.immich.rule=Host(`photos.${DOMAIN:-localhost.me}`)
      - traefik.http.routers.immich.entrypoints=websecure
      - traefik.http.services.immich.loadbalancer.server.port=2283
      - homepage.group=Media & Files
      - homepage.name=Immich
      - homepage.icon=immich.png
      - homepage.href=https://photos.${DOMAIN:-localhost.me}
      - homepage.description=Family photo and video library
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M

  # Background workers
  immich-microservices:
    profiles: [apps, all]
    depends_on:
      immich-postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    image: ghcr.io/immich-app/immich-server:v2.7.3
    command: [start.sh, microservices]
    volumes: [immich_library:/usr/src/app/upload]
    environment:
      - DB_HOSTNAME=immich-postgres
      - DB_USERNAME=${IMMICH_DB_USERNAME:-immich}
      - DB_PASSWORD=${IMMICH_DB_PASSWORD:?Generate a password using `openssl rand
        -base64 32 2>/dev/null`}
      - DB_DATABASE_NAME=${IMMICH_DB_DATABASE_NAME:-immich}
      - REDIS_HOSTNAME=redis
      - IMMICH_WORKERS_EXCLUDE=api
    networks: [immich]
    healthcheck:
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 60s
    restart: unless-stopped
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '1'
    #       memory: 512M
    #     reservations:
    #       cpus: '0.5'
    #       memory: 256M

  # ML processing - always on
  immich-machine-learning:
    profiles: [apps, all]
    depends_on:
      redis:
        condition: service_healthy
    image: ghcr.io/immich-app/immich-machine-learning:v2.7.3
    volumes: [immich_model_cache:/cache]
    environment: [REDIS_HOSTNAME=redis]
    networks: [immich]
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          cpus: '1'
          memory: 2G

  # Queue storage - Always on
  redis:
    profiles: [apps, all]
    image: redis:7.4.8-alpine3.21
    volumes: [immich_redis:/data]
    networks: [immich]
    healthcheck:
      test: [CMD, redis-cli, ping]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.25'
          memory: 128M
networks:
  immich:
  traefik_public:
    name: traefik_public
    driver: bridge
    attachable: true
volumes:
  immich_library:
  immich_postgres_data:
  immich_model_cache:
  immich_redis:
  immich_power_tools_data:
