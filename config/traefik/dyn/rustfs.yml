---
http:
  routers:
    rustfs-api:
      rule: Host(`rustfs-api.{{ env "DOMAIN" }}`)
      entrypoints: ['{{ env "TRAEFIK_ENTRYPOINT" }}']
      service: rustfs-api
      middlewares: [startup-retry@file]
    rustfs-console:
      rule: Host(`rustfs.{{ env "DOMAIN" }}`)
      entrypoints: ['{{ env "TRAEFIK_ENTRYPOINT" }}']
      service: rustfs-console
      middlewares: [startup-retry@file]
  services:
    rustfs-api:
      loadBalancer:
        servers:
          - url: http://rustfs:9000/
    rustfs-console:
      loadBalancer:
        servers:
          - url: http://rustfs:9001/
