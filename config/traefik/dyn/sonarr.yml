---
http:
  routers:
    sonarr:
      rule: Host(`sonarr.{{ env "DOMAIN" }}`)
      entrypoints: [websecure]
      service: sonarr
      middlewares: [startup-retry@file]
  services:
    sonarr:
      loadBalancer:
        servers:
          - url: http://gluetun:8989/
