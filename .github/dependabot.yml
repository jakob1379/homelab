---
version: 2
updates:
  - package-ecosystem: docker-compose
    directories: [/, /services, /home-assistant]
    schedule:
      interval: weekly
    groups:
      docker-compose-images:
        patterns: ['*']
    commit-message:
      prefix: chore(deps)
    labels: [dependencies, docker]
    open-pull-requests-limit: 10
